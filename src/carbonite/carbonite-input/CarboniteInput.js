import React from 'react';
import styled from 'styled-components';

const Input = styled.input`
  width: 100%;
  padding:10px 7px;
  border: none;
`;

const CarboniteInput = () => (
  <Input />
);

export default CarboniteInput;